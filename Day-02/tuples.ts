/**
 * ======== TypeScript tuples ========
 * A tuple is a TypeScript type that works like an array with some additional considerations.
 * A tuple type is a fixed-length array where the elements have fixed types.
 * The number of elements in the tuple is fixed, and the types of elements are known.
 * The elements of the tuple can be of different types.
 * The elements of the tuple can be accessed using the index.
 * The elements of the tuple can be assigned to variables.
 */

let t1: [string, number] = ['John', 25];
let t2: [number, string] = [ 21 , "charaka"];

console.log(t1)